/**
 * Build-time globals substituted by Vite (see `electron.vite.config.ts`
 * `renderer.define`). Declared as plain types so consumers don't need
 * `typeof` guards — the substitution is a compile-time constant.
 */
declare const __APP_VERSION__: string;
