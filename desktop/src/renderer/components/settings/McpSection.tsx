/**
 * MCP Server section — placeholder during the Phase 2 redesign.
 *
 * Task 7 replaced the old single-client (Claude Desktop only) UI with
 * a stub; Task 8 will land the full multi-client Settings →
 * Integrations sub-page (Claude Desktop / Code / Cursor / Pi).
 */
export function McpSection() {
  return (
    <div className="text-sm text-muted-foreground">
      MCP integration UI under refactor — see Settings → Integrations (coming in Task 8).
    </div>
  );
}
