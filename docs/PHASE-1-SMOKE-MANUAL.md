# Phase 1 Manual GUI Smoke — `phase-1d` Pre-Tag Checklist

**Date:** 2026-05-14
**Goal:** Verify the full Confirm flow end-to-end for all 5 extraction stages, exercising the real LLM, real PDF parsing, real renderer/IPC/main pipeline, and the EF Matcher v1 recommended panel.
**Outcome:** if all 5 stages pass, tag `phase-1d`.

## Why this is manual

The automated GUI smoke (Playwright E2E) was deferred — getting Playwright + Electron + TanStack Router to play nicely turned into a multi-day investment that didn't fit the Phase 1 closing budget. The E2E foundation (`tests/e2e/_setup.ts`, harness, canned data, dev-collision guard) is shipped on `main` for a future Phase 1.5 / Phase 2 sub-project to pick up. For now, the consolidated 5-stage smoke is walked manually.

## Fixtures

All 5 fixtures live in `tests/fixtures/smoke/` and are **synthetic** invoices generated by `scripts/generate-smoke-fixtures.mjs`. They render filled-in Chinese invoice HTML through Playwright's headless Chromium — every required schema field has a plausible value, no privacy concerns, fully reproducible.

> **Regenerate any time:** `pnpm exec node scripts/generate-smoke-fixtures.mjs`
> Re-runs are deterministic — same content every time.

| File | Stage | Pre-filled fields |
|---|---|---|
| `01-utility-sample.pdf` | `china_utility.v1` | 供电公司=国家电网北京电力公司海淀分公司, 户号=1000-1234-5678, 计费周期=2026-04-01 至 2026-04-30, 用电量=1234.56 kWh, 应缴电费=678.50 元 |
| `02-fuel-receipt-sample.pdf` | `fuel_receipt.v1` | 中石化北京海淀加油站, 0号柴油, 45.60 升, 单价 7.85 元/升, 合计 357.96 元, 京A·12345 |
| `03-freight-sample.pdf` | `freight.v1` | 顺丰速运北京分公司, 公路运输（重型柴油货车）, 北京→上海, 1500 kg, 6.5 m³, 运单 SF1234567890CN, 运费 2800.00 元 |
| `04-purchase-sample.pdf` | `purchase.v1` | 宝山钢铁销售分公司, 冷轧钢板, 5000 kg × 5.00 元/kg = 25000.00 元（增值税专用发票） |
| `05-travel-sample.pdf` | `travel.v1` | 中国国际航空, 张三, CA1234, 经济舱, 北京首都 → 上海虹桥, 2026-04-25 08:30, 合计 1200.00 元 |

## Pre-flight

Before starting:

- [ ] `git status` shows clean working tree on `main` at the latest commit.
- [ ] `pnpm rebuild better-sqlite3` (if the last thing you did was an E2E or production build — flips the binary back to Node ABI so dev mode works).
- [ ] Confirm an OpenAI API key is reachable. The recommender hits `gpt-4o-mini`; without a key, the recommended panel stays empty (the test then verifies graceful fallback to FTS-sorted list).
- [ ] Optionally: delete `~/Library/Application Support/carbonbook/app.sqlite` if you want a fresh DB. (Or skip this — using your existing org/sources is fine; the test just adds 5 new activity_data rows.)
- [ ] Start the app: `pnpm dev`. Wait for the Electron window to appear.

## Per-stage checklist

For each of the 5 stages: upload the fixture → run extraction → review the review page → pick an emission source → verify the recommended panel → click a recommended EF → submit → verify dashboard reflects the new activity_data row.

> **Tip:** if you have a clean DB (no org yet), the wizard will run first. Complete onboarding once, then this checklist starts after the dashboard appears.

### Stage 1 — `china_utility.v1` (电网用电)

- [ ] Navigate to `/documents`.
- [ ] Pick stage **china_utility.v1** in the upload dropdown.
- [ ] Upload `tests/fixtures/smoke/01-utility-sample.pdf`.
- [ ] Wait for extraction (5-15 sec; vision model is slower than text).
- [ ] Click the document row → opens `/documents/<id>` review page.
- [ ] **Verify extraction fields** match the fixture:
  - `supplier_name` ≈ "国家电网北京电力公司海淀分公司"
  - `account_no` ≈ "1000-1234-5678"
  - `amount_kwh` ≈ 1234.56
  - `amount_yuan` ≈ 678.50
  - `period_start` ≈ "2026-04-01", `period_end` ≈ "2026-04-30"
  - `confidence` should be `high`
- [ ] Click **Confirm** ("确认 → 记为活动数据").
- [ ] In the embedded ActivityForm, pick (or create) an **emission source** with `scope=2`, `category=electricity.grid`.
- [ ] **Verify "为本单据推荐" panel renders** with up to 3 starred EF recommendations. Each should have a Chinese reasoning sentence.
- [ ] Click the first starred EF radio.
- [ ] Form should prefill: `unit=kWh`, `amount=1234.56`.
- [ ] Click **Submit** ("记录活动").
- [ ] You should navigate to `/` (dashboard). The new activity_data row appears with a non-zero CO₂e.

### Stage 2 — `fuel_receipt.v1` (加油发票)

- [ ] Navigate back to `/documents`.
- [ ] Pick stage **fuel_receipt.v1**.
- [ ] Upload `tests/fixtures/smoke/02-fuel-receipt-sample.pdf`.
- [ ] Wait for extraction.
- [ ] Click the new document row.
- [ ] **Verify**:
  - `supplier_name` ≈ "中国石化销售股份有限公司北京海淀加油站"
  - `fuel_type` ≈ "0号柴油" (or similar); `fuel_category` ≈ "diesel"
  - `volume_l` ≈ 45.60
  - `unit_price_yuan` ≈ 7.85
  - `amount_yuan` ≈ 357.96
  - `occurred_at` ≈ "2026-04-15"
  - `license_plate` ≈ "京A·12345" (or "京A12345")
- [ ] Click Confirm → pick or create an emission source with `scope=1`, `category=fuel.mobile`.
- [ ] **Recommended panel** should surface `fuel.diesel.combustion.global.2024` first.
- [ ] Pick the first starred EF.
- [ ] Form prefills `unit=L`, `amount=45.60`.
- [ ] Submit → dashboard updates.

### Stage 3 — `freight.v1` (货运)

- [ ] Navigate to `/documents`.
- [ ] Pick stage **freight.v1**.
- [ ] Upload `tests/fixtures/smoke/03-freight-sample.pdf`.
- [ ] Wait for extraction.
- [ ] Click the new row.
- [ ] **Verify**:
  - `supplier_name` ≈ "顺丰速运有限公司北京分公司"
  - `mode` ≈ "road"
  - `vehicle_class` ≈ "重型柴油货车"
  - `weight_kg` ≈ 1500
  - `volume_m3` ≈ 6.5
  - `origin` ≈ "北京...天竺..." / `destination` ≈ "上海...外高桥..."
  - `tracking_no` ≈ "SF1234567890CN"
  - `amount_yuan` ≈ 2800.00
  - `distance_km` will be null (Phase 2 routing API work)
- [ ] Click Confirm → emission source with `scope=3`, `category=freight.road` (prefix-match means `freight.road` pulls in all `freight.road.*`).
- [ ] **Recommended panel** should surface `freight.road.heavy_diesel_truck` or `freight.road.generic` first.
- [ ] Pick first starred EF.
- [ ] Form prefills `unit=kg`, `amount=1500`. (raw weight; tonne-km conversion is Phase 2.)
- [ ] Submit → dashboard.

### Stage 4 — `purchase.v1` (采购发票)

- [ ] Navigate to `/documents`.
- [ ] Pick stage **purchase.v1**.
- [ ] Upload `tests/fixtures/smoke/04-purchase-sample.pdf`.
- [ ] Wait for extraction.
- [ ] Click the new row.
- [ ] **Verify**:
  - `supplier_name` ≈ "宝山钢铁股份有限公司销售分公司"
  - `item_description` ≈ "冷轧钢板"
  - `category` ≈ "raw_material"
  - `quantity_kg` ≈ 5000
  - `amount_yuan` ≈ 25000.00
  - `occurred_at` ≈ "2026-04-22"
- [ ] Click Confirm → emission source with `scope=3, category=purchase.material` (the prefix-match catches `purchase.material.steel_primary` and `purchase.material.paper_office`).
- [ ] **Recommended panel** should surface `purchase.material.steel_primary` first.
- [ ] Pick first starred EF.
- [ ] Form prefills `unit=kg`, `amount=5000`. (Because `quantity_kg > 0`, the dual-track logic chooses kg-unit over CNY-unit.)
- [ ] Submit → dashboard.

### Stage 5 — `travel.v1` (差旅票据)

- [ ] Navigate to `/documents`.
- [ ] Pick stage **travel.v1**.
- [ ] Upload `tests/fixtures/smoke/05-travel-sample.pdf`.
- [ ] Wait for extraction.
- [ ] Click the new row.
- [ ] **Verify**:
  - `supplier_name` ≈ "中国国际航空股份有限公司"
  - `mode` ≈ "air"
  - `passenger_name` ≈ "张三"
  - `origin` ≈ "北京首都国际机场" / `destination` ≈ "上海虹桥国际机场"
  - `departure_at` ≈ "2026-04-25T08:30" (or "2026-04-25 08:30")
  - `arrival_at` ≈ "2026-04-25T10:50"
  - `travel_class` ≈ "经济舱" / `flight_or_train_no` ≈ "CA1234"
  - `amount_yuan` ≈ 1200.00
  - `ticket_no` ≈ "999-1234567890"
  - `distance_km` will be null (Phase 2 work)
- [ ] Click Confirm → emission source with `scope=3, category=travel.air` (prefix-match catches all `travel.air.*`).
- [ ] **Recommended panel** should surface `travel.air.economy.shorthaul` first (Beijing→Shanghai is short-haul).
- [ ] Pick first starred EF.
- [ ] Form prefills `unit=passenger-km`, `amount=1` (default since distance_km is null). Adjust manually to ~1200 km if you want a realistic CO₂e figure.
- [ ] Submit → dashboard.

## Cross-stage checks

After all 5 stages confirmed:

- [ ] Dashboard shows **5 new activity_data rows** with non-zero CO₂e contributions.
- [ ] Scope cards updated: scope 1 (fuel) + scope 2 (utility) + scope 3 (freight + purchase + travel) should all have non-zero values.
- [ ] `/activities` page lists all 5 entries with correct stage attribution.
- [ ] `/documents` page shows all 5 uploaded documents with "已抽取" / "Parsed" status chips.
- [ ] Spot-check one entry's emission factor link → the EF detail / pin status looks correct.

## Failure modes — when to NOT tag

If any of these happens, **do not tag** — file an issue or kick off a fix sub-project instead:

- ❌ Extraction returns 500 or hangs > 60 sec on any stage.
- ❌ The Confirm button doesn't reveal the ActivityForm.
- ❌ Picking an emission source doesn't fire the recommended panel query (network failure aside).
- ❌ The recommended panel never disappears even when LLM fails (should fall back to FTS list silently).
- ❌ Submitting the form doesn't create an activity_data row, OR creates one with `co2e_kg = 0` (some EF/unit mismatch is real and worth investigating).
- ❌ Dashboard scope cards don't update after a successful Confirm.

## Pass = tag

If everything above is green:

```bash
cd /Users/lxz/ws/personal/carbonbook
git tag phase-1d
git push --tags
```

That closes Phase 1 — 4 extraction stages added beyond `phase-1a`'s baseline, per-stage component split, EF Matcher v1, FTS5 + 32 seeded EFs, full IPC + ActivityForm UX. **Next:** Phase 2 (questionnaire side + MCP integration) brainstorm.

## Appendix: how the fixtures are generated

`scripts/generate-smoke-fixtures.mjs` defines 5 inline HTML templates (each a one-page A4 invoice with realistic Chinese typography + sample data), launches Playwright's headless Chromium, and renders each template to a PDF in `tests/fixtures/smoke/`.

Why synthetic rather than real:

- **Privacy:** real invoices contain account numbers, company names, addresses.
- **Repeatability:** anyone can regen with `pnpm exec node scripts/generate-smoke-fixtures.mjs` — output is deterministic.
- **Coverage:** every required schema field is pre-filled, so extraction has unambiguous targets.

Why this is good enough:

- carbonbook's stage schemas are "permissive" (any missing field defaults to null; the renderer surfaces what was actually read).
- The point of the smoke is to verify the **pipeline** end-to-end, not to grade extraction accuracy on adversarial real-world inputs.
- Stage-specific prompt wrappers (`<bill>`, `<receipt>`, `<invoice>`, `<ticket>`) train the LLM to focus on the right invoice shape regardless of synthetic vs. real.

If you want stronger confidence in extraction quality on real invoices, that's a separate stress test — Phase 2 territory.
